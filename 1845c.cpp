#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string s;
        cin >> s;
        int m;
        cin >> m;
        string l, r;
        cin >> l >> r;

        vector<int> idx(10);

        for(int i=0;i<s.length();i++){
            
        }

    }

    return 0;
}